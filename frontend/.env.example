# Optional: MCP client request timeouts (milliseconds).
# The SDK default is 60000 (60s), which is too short for orchestrate_autofix.
# VITE_MCP_ORCHESTRATE_TIMEOUT_MS=900000
# VITE_MCP_RESOLVE_TIMEOUT_MS=120000
